---
import { Image } from "astro:assets";
import profilePic from "../assets/2023-03-17_square_near_profilepic.jpeg";
import { getLocalizedResumeContent, type ResumeLocale } from "../data/cv";
import CVIntro from "./CVIntro.astro";

interface Props {
  locale?: ResumeLocale;
}

const { locale = "de" } = Astro.props;
const content = getLocalizedResumeContent(locale);
const toolbarAriaLabel = locale === "de" ? "Lebenslauf-Aktionen" : "CV actions";
const profileImageAlt =
  locale === "de"
    ? "Porträt von Benjamin Jesuiter"
    : "Portrait of Benjamin Jesuiter";
const languageSwitchHref = locale === "de" ? "/en/cv/" : "/de/lebenslauf/";
---

<main class="resume-page">
  <div class="resume-toolbar print-hidden" aria-label={toolbarAriaLabel}>
    <div class="resume-toolbar-shell">
      <div class="resume-toolbar-copy">
        <p class="resume-toolbar-kicker">{content.badge}</p>
        <p class="resume-toolbar-note">{content.printHint}</p>
      </div>

      <div class="resume-toolbar-actions">
        <a
          href={languageSwitchHref}
          class="resume-action resume-action-secondary"
        >
          {content.navigation.languageSwitchLabel}
        </a>
        <button
          type="button"
          class="resume-action resume-action-print"
          onclick="window.__resumePrintMode('print')"
        >
          {content.navigation.printLabel}
        </button>
        <button
          type="button"
          class="resume-action resume-action-download"
          onclick="window.__resumePrintMode('download')"
        >
          {content.navigation.downloadLabel}
        </button>
      </div>
    </div>
  </div>

  <article class="resume-sheet">
    <header class="resume-hero">
      <div class="resume-hero-copy">
        <p class="resume-badge">{content.badge}</p>
        <h1>{content.heading}</h1>
        <CVIntro locale={locale} />
      </div>

      <div class="resume-hero-media">
        <Image
          src={profilePic}
          alt={profileImageAlt}
          loading="eager"
          class="resume-hero-image"
        />
      </div>
    </header>

    <section class="resume-grid resume-top-grid">
      <section class="resume-panel resume-panel-emphasis">
        <h2>{content.factsTitle}</h2>
        <dl class="resume-facts">
          {
            content.facts.map((fact) => (
              <div>
                <dt>{fact.label}</dt>
                <dd class="text-balance">{fact.value}</dd>
              </div>
            ))
          }
        </dl>
      </section>

      <section
        class="resume-panel resume-panel-emphasis-mirrored py-[1.1rem] pr-[1.16rem] pl-8"
      >
        <h2>{content.contactTitle}</h2>
        <ul class="resume-link-list">
          {
            content.contactLinks.map((link) => (
              <li>
                <a href={link.href} target="_blank" rel="noreferrer">
                  <span>{link.label}</span>
                  <span>{link.href.replace(/^https?:\/\//, "")}</span>
                </a>
              </li>
            ))
          }
        </ul>

        <h2 class="resume-subheading">{content.industriesTitle}</h2>
        <ul class="resume-chip-list">
          {content.industries.map((industry) => <li>{industry}</li>)}
        </ul>
      </section>
    </section>

    <section class="resume-panel resume-section">
      <div class="resume-section-heading">
        <h2>{content.skillsTitle}</h2>
      </div>

      <div class="resume-skills-grid">
        {
          content.skillCategories.map((category, index) => (
            <section class="resume-skill-card">
              <div class="resume-card-heading">
                <p class="resume-card-index">
                  {String(index + 1).padStart(2, "0")}
                </p>
                <h3>{category.title}</h3>
              </div>
              <ul>
                {category.items.map((item) =>
                  typeof item === "string" ? (
                    <li>{item}</li>
                  ) : "items" in item ? (
                    <li class="resume-skill-group">
                      <span class="resume-skill-group-label">{item.label}</span>
                      <ul class="resume-skill-sublist">
                        {item.items.map((subItem) => (
                          <li class="resume-skill-sublist-item">{subItem}</li>
                        ))}
                      </ul>
                    </li>
                  ) : (
                    <li class="resume-skill-chip-row">
                      <span>{item.text}</span>
                      <span class="resume-chip">{item.chip}</span>
                    </li>
                  ),
                )}
              </ul>
            </section>
          ))
        }
      </div>
    </section>

    <section class="resume-section">
      <div class="resume-section-heading">
        <div>
          <h2>{content.projectsTitle}</h2>
          <p>{content.projectsIntro}</p>
        </div>
      </div>

      <div class="resume-projects">
        {
          content.projects.map((project, index) => (
            <article class="resume-project-card">
              <header class="resume-project-header">
                <div class="resume-project-title-block">
                  <div class="resume-project-eyebrow">
                    <span class="resume-card-index">
                      {String(index + 1).padStart(2, "0")}
                    </span>
                    <p class="resume-project-period">{project.period}</p>
                  </div>
                  <p class="resume-project-sector">{project.sector}</p>
                  <h3>{project.title}</h3>
                </div>

                <dl class="resume-project-meta">
                  {project.duration && (
                    <div>
                      <dt>{content.durationLabel}</dt>
                      <dd>{project.duration}</dd>
                    </div>
                  )}
                  {project.teamSize && (
                    <div>
                      <dt>{content.teamSizeLabel}</dt>
                      <dd>{project.teamSize}</dd>
                    </div>
                  )}
                  <div>
                    <dt>{content.rolesLabel}</dt>
                    <dd>{project.roles.join(" · ")}</dd>
                  </div>
                </dl>
              </header>

              <div class="resume-project-body">
                <div class="resume-project-copy">
                  {project.summary.map((paragraph) => (
                    <p>{paragraph}</p>
                  ))}
                </div>

                <section class="resume-detail-card">
                  <h4>{content.highlightsTitle}</h4>
                  <ul>
                    {project.highlights.map((highlight) => (
                      <li>{highlight}</li>
                    ))}
                  </ul>
                </section>

                <section class="resume-detail-card">
                  <h4>{content.stackTitle}</h4>
                  <dl class="resume-stack-list">
                    {project.stack.map((item) => (
                      <div>
                        <dt>{item.label}</dt>
                        <dd>{item.value}</dd>
                      </div>
                    ))}
                  </dl>
                </section>
              </div>
            </article>
          ))
        }
      </div>
    </section>
  </article>
</main>

<script is:inline>
  (() => {
    const setMode = (mode) => {
      document.documentElement.dataset.resumeOutput = mode;
      document.body?.setAttribute("data-resume-output", mode);
    };

    const clearMode = () => {
      delete document.documentElement.dataset.resumeOutput;
      document.body?.removeAttribute("data-resume-output");
    };

    window.__resumePrintMode = (mode) => {
      setMode(mode);
      window.print();
    };

    window.addEventListener("afterprint", clearMode);
  })();
</script>

<style is:global>
  .resume-page {
    --resume-page-padding: clamp(1rem, 2.4vw, 2.1rem);
    --resume-dock-overlap: 5.8rem;
    --resume-divider: rgba(167, 243, 208, 0.12);
    --resume-divider-strong: rgba(167, 243, 208, 0.2);
    --resume-panel-radius: 14px;
    --resume-panel-border: rgba(167, 243, 208, 0.14);
    --resume-panel-border-strong: rgba(167, 243, 208, 0.22);
    --resume-panel-surface: rgba(4, 23, 18, 0.84);
    --resume-panel-surface-strong: rgba(6, 28, 21, 0.9);
    min-height: 100vh;
    margin-top: calc(var(--resume-dock-overlap) * -1);
    padding: calc(var(--resume-page-padding) + var(--resume-dock-overlap))
      var(--resume-page-padding) var(--resume-page-padding);
    background: #02110d;
    color: #ecfdf5;
  }

  .resume-toolbar {
    width: min(1140px, 100%);
    margin: 0 auto 1rem;
  }

  .resume-toolbar-shell {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding: 0.85rem 1rem;
    border-radius: 20px;
    border: 1px solid rgba(167, 243, 208, 0.18);
    background: rgba(3, 21, 16, 0.8);
    box-shadow:
      0 16px 34px rgba(1, 10, 7, 0.18),
      inset 0 1px 0 rgba(255, 255, 255, 0.04);
    backdrop-filter: blur(16px);
  }

  .resume-toolbar-copy {
    display: grid;
    gap: 0.22rem;
    max-width: 54rem;
  }

  .resume-toolbar-kicker {
    margin: 0;
    font-family: "Chakra Petch", sans-serif;
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: #86efac;
  }

  .resume-toolbar-note {
    margin: 0;
    line-height: 1.55;
    color: #c7f3df;
  }

  .resume-toolbar-actions {
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-end;
    gap: 0.75rem;
  }

  .resume-action {
    appearance: none;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-height: 3rem;
    border-radius: 999px;
    padding: 0.7rem 1.1rem;
    border: 1px solid transparent;
    font-family: "Chakra Petch", sans-serif;
    font-size: 0.88rem;
    font-weight: 700;
    letter-spacing: 0.06em;
    text-transform: uppercase;
    text-decoration: none;
    cursor: pointer;
    transition:
      transform 170ms ease,
      border-color 170ms ease,
      filter 170ms ease,
      background-color 170ms ease,
      color 170ms ease,
      box-shadow 170ms ease;
  }

  .resume-action:hover {
    transform: translateY(-1px);
  }

  .resume-action-secondary {
    color: #d8fbed;
    border-color: rgba(167, 243, 208, 0.18);
    background: rgba(7, 34, 26, 0.56);
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.04);
  }

  .resume-action-secondary:hover {
    border-color: rgba(110, 231, 183, 0.5);
    background: rgba(8, 39, 29, 0.82);
  }

  .resume-action-download {
    color: #032015;
    border: 1px solid rgba(255, 255, 255, 0.24);
    background: #9ef0c7;
    box-shadow: 0 10px 24px rgba(0, 0, 0, 0.2);
  }

  .resume-action-download:hover {
    filter: brightness(1.04);
    box-shadow: 0 14px 28px rgba(0, 0, 0, 0.28);
  }

  .resume-action-print {
    color: #d8fbed;
    border: 1px solid rgba(167, 243, 208, 0.28);
    background: rgba(3, 24, 18, 0.72);
    box-shadow:
      0 12px 26px rgba(1, 10, 7, 0.24),
      inset 0 1px 0 rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(18px);
  }

  .resume-action-print:hover {
    border-color: #6ee7b7;
    background: rgba(5, 30, 22, 0.92);
  }

  .resume-sheet {
    width: min(1140px, 100%);
    margin: 0 auto;
    padding: clamp(1.1rem, 2vw, 1.8rem);
    border-radius: 22px;
    overflow: hidden;
    border: 1px solid rgba(167, 243, 208, 0.18);
    background:
      linear-gradient(90deg, rgba(220, 252, 231, 0.015) 1px, transparent 1px) 0
        0 / 34px 100%,
      rgba(4, 21, 17, 0.92);
    box-shadow: 0 24px 52px rgba(1, 10, 7, 0.42);
  }

  .resume-hero {
    display: grid;
    gap: 1.15rem;
    padding-bottom: 1.6rem;
    border-bottom: 1px dashed var(--resume-divider-strong);
  }

  .resume-hero-copy {
    display: grid;
    gap: 0.95rem;
    align-content: start;
  }

  .resume-badge {
    margin: 0;
    font-family: "Chakra Petch", sans-serif;
    font-size: 0.84rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: #6ee7b7;
  }

  .resume-hero h1,
  .resume-section h2,
  .resume-panel h2,
  .resume-project-card h3,
  .resume-detail-card h4,
  .resume-skill-card h3 {
    font-family: "Fraunces", serif;
    text-wrap: balance;
  }

  .resume-hero h1 {
    margin: 0;
    font-size: clamp(2.4rem, 6vw, 4.6rem);
    line-height: 0.92;
    letter-spacing: -0.04em;
    color: #ecfdf5;
  }

  .resume-intro {
    max-width: 72ch;
    margin: 0;
    font-size: clamp(1rem, 1.5vw, 1.08rem);
    line-height: 1.75;
    color: #c7f3df;
  }

  .resume-intro p {
    margin: 0;
  }

  .resume-intro p + p {
    margin-top: 0.45rem;
  }

  .resume-intro strong {
    color: #ecfdf5;
  }

  .resume-hero-media {
    position: relative;
    min-height: 20rem;
    border-radius: 28px;
    overflow: hidden;
    border: 1px solid rgba(167, 243, 208, 0.18);
    background: rgba(5, 23, 19, 0.82);
    box-shadow: 0 24px 48px rgba(1, 10, 7, 0.28);
  }

  .resume-hero-media::after {
    content: "";
    position: absolute;
    inset: 0;
    background: linear-gradient(
      180deg,
      rgba(3, 16, 12, 0.02) 0%,
      rgba(4, 28, 19, 0.18) 70%,
      rgba(3, 16, 12, 0.5) 100%
    );
    pointer-events: none;
  }

  .resume-hero-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center top;
    filter: saturate(0.88) contrast(1.04) brightness(1);
  }

  .resume-grid {
    display: grid;
    gap: 1rem;
  }

  .resume-top-grid {
    margin-top: 1rem;
  }

  .resume-top-grid > * + * {
    padding-top: 0;
    border-top: 0;
  }

  .resume-panel,
  .resume-project-card,
  .resume-skill-card {
    position: relative;
    overflow: hidden;
    border-radius: var(--resume-panel-radius);
    border: 1px solid var(--resume-panel-border);
    background: var(--resume-panel-surface);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.04),
      0 14px 28px rgba(1, 10, 7, 0.16);
    backdrop-filter: blur(8px);
  }

  .resume-panel::before,
  .resume-panel::after,
  .resume-project-card::before,
  .resume-project-card::after,
  .resume-skill-card::before,
  .resume-skill-card::after {
    display: none;
  }

  .resume-panel {
    padding: 1.1rem;
  }

  .resume-panel-emphasis,
  .resume-panel-emphasis-mirrored {
    background: var(--resume-panel-surface-strong);
  }

  .resume-panel-emphasis::before,
  .resume-panel-emphasis::after,
  .resume-panel-emphasis-mirrored::before,
  .resume-panel-emphasis-mirrored::after,
  .resume-project-card::before,
  .resume-project-card::after {
    border-color: var(--resume-panel-border-strong);
  }

  .resume-panel h2,
  .resume-section h2 {
    margin: 0;
    font-size: clamp(1.35rem, 2vw, 1.9rem);
    color: #ecfdf5;
  }

  .resume-subheading {
    margin-top: 1.35rem !important;
  }

  .resume-facts {
    display: grid;
    gap: 0.9rem;
    margin-top: 1.05rem;
  }

  .resume-facts div,
  .resume-stack-list div {
    display: grid;
    gap: 0.2rem;
  }

  .resume-facts div + div,
  .resume-stack-list div + div {
    border-top: 1px dashed var(--resume-divider);
    padding-top: 0.7rem;
  }

  .resume-facts dt,
  .resume-project-meta dt,
  .resume-stack-list dt {
    font-family: "Chakra Petch", sans-serif;
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: #6ee7b7;
  }

  .resume-facts dd,
  .resume-project-meta dd,
  .resume-stack-list dd {
    margin: 0;
    line-height: 1.6;
    color: #d7fbec;
  }

  .resume-link-list,
  .resume-chip-list,
  .resume-skill-card ul,
  .resume-detail-card ul {
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .resume-link-list {
    display: grid;
    gap: 0.7rem;
    margin-top: 1.05rem;
  }

  .resume-link-list li + li {
    border-top: 1px dashed var(--resume-divider);
    padding-top: 0.7rem;
  }

  .resume-link-list a {
    display: grid;
    gap: 0.22rem;
    padding: 0.72rem 0.85rem;
    border-radius: 16px;
    border: 1px solid rgba(167, 243, 208, 0.1);
    background: rgba(4, 20, 16, 0.46);
    transition:
      color 170ms ease,
      transform 170ms ease,
      border-color 170ms ease,
      background-color 170ms ease;
  }

  .resume-link-list li + li {
    border-top: 0;
    padding-top: 0;
  }

  .resume-link-list a:hover {
    transform: translateX(2px);
    border-color: rgba(110, 231, 183, 0.28);
    background: rgba(6, 31, 24, 0.56);
  }

  .resume-link-list a span:first-child {
    font-family: "Chakra Petch", sans-serif;
    font-size: 0.75rem;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: #6ee7b7;
  }

  .resume-link-list a span:last-child {
    color: #d7fbec;
    line-height: 1.6;
    word-break: break-word;
  }

  .resume-chip-list {
    display: flex;
    flex-wrap: wrap;
    gap: 0.55rem;
    margin-top: 1rem;
  }

  /*
    Keep these pills visually explicit.
    A lighter/softer treatment made the industries read like plain wrapped text,
    while this stronger background + border makes the intended tag grouping clear.
    Reuse this approach if similar pill lists start looking too subtle again.
  */
  .resume-chip-list li {
    padding: 0.5rem 0.8rem;
    border-radius: 999px;
    background: rgba(4, 20, 16, 0.52);
    color: #ecfdf5;
    border: 1px solid rgba(110, 231, 183, 0.24);
    line-height: 1.4;
  }

  .resume-section {
    margin-top: 1rem;
    padding-top: 1rem;
    border-top: 1px dashed var(--resume-divider-strong);
  }

  .resume-panel.resume-section {
    padding-bottom: 1rem;
  }

  .resume-panel.resume-section + .resume-section {
    padding-top: 1rem;
    border-top: 1px dashed var(--resume-divider-strong);
  }

  .resume-section-heading {
    margin-bottom: 1rem;
  }

  .resume-section-heading p {
    margin: 0.45rem 0 0;
    line-height: 1.6;
    color: #bdeed8;
  }

  .resume-skills-grid {
    display: grid;
    gap: 0.9rem 1rem;
  }

  .resume-card-heading {
    display: flex;
    align-items: baseline;
    gap: 0.7rem;
    margin-bottom: 0.85rem;
  }

  .resume-card-index {
    margin: 0;
    font-family: "Chakra Petch", sans-serif;
    font-size: 0.74rem;
    font-weight: 700;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: #86efac;
    opacity: 0.92;
  }

  .resume-skill-card {
    padding: 1rem 1rem 1.05rem;
    border-radius: 18px;
    border: 1px solid rgba(167, 243, 208, 0.12);
    background: rgba(4, 20, 16, 0.5);
    box-shadow:
      inset 0 1px 0 rgba(255, 255, 255, 0.04),
      0 12px 24px rgba(1, 10, 7, 0.14);
    backdrop-filter: blur(8px);
  }

  .resume-skill-card::before,
  .resume-skill-card::after {
    display: none;
  }

  .resume-skill-card h3 {
    margin: 0;
    font-size: 1.08rem;
    color: #ecfdf5;
  }

  .resume-skill-card ul {
    display: grid;
    gap: 0.46rem;
  }

  .resume-skill-group-label {
    font-weight: 500;
    color: #ddfced;
  }

  .resume-skill-sublist {
    display: flex;
    flex-wrap: wrap;
    row-gap: 0.2rem;
    column-gap: 0.35rem;
    margin-top: 0.32rem;
    margin-left: 0;
    padding-left: 0;
  }

  .resume-skill-sublist .resume-skill-sublist-item {
    list-style: none;
    position: static;
    padding-left: 0;
    line-height: 1.35;
    font-size: 0.9em;
  }

  .resume-skill-sublist .resume-skill-sublist-item::before {
    content: "·";
    position: static;
    display: inline-block;
    width: auto;
    height: auto;
    margin-right: 0.35rem;
    border-radius: 0;
    background: none;
    color: rgba(110, 231, 183, 0.85);
  }

  .resume-skill-chip-row {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.45rem;
  }

  .resume-chip {
    display: inline-block;
    border: 1px solid rgba(110, 231, 183, 0.24);
    background: rgba(16, 185, 129, 0.08);
    color: rgba(187, 247, 208, 0.8);
    border-radius: 999px;
    padding: 0.08rem 0.45rem;
    font-size: 0.7rem;
    font-weight: 600;
    line-height: 1.3;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .resume-skill-card li,
  .resume-detail-card li,
  .resume-project-copy p {
    line-height: 1.7;
    color: #c7f3df;
  }

  .resume-skill-card li,
  .resume-detail-card li {
    position: relative;
    padding-left: 1rem;
  }

  .resume-skill-card li::before,
  .resume-detail-card li::before {
    content: "";
    position: absolute;
    left: 0;
    top: 0.72rem;
    width: 0.38rem;
    height: 0.38rem;
    border-radius: 999px;
    background: #6ee7b7;
  }

  .resume-skill-card li.resume-skill-group::before {
    top: 0.78rem;
    width: 0.26rem;
    height: 0.26rem;
  }

  .resume-projects {
    display: grid;
    gap: 1.15rem;
  }

  .resume-project-card {
    background:
      linear-gradient(
          180deg,
          rgba(110, 231, 183, 0.82),
          rgba(110, 231, 183, 0.18)
        )
        left top / 2px 100% no-repeat,
      rgba(4, 21, 17, 0.78);
  }

  .resume-project-header {
    display: grid;
    gap: 1rem;
    padding: 1.18rem 1.18rem 1.04rem 1.3rem;
    border-bottom: 1px dashed var(--resume-divider-strong);
    background: rgba(6, 27, 21, 0.88);
  }

  .resume-project-title-block {
    display: grid;
    gap: 0.45rem;
  }

  .resume-project-eyebrow {
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    gap: 0.65rem;
  }

  .resume-project-period,
  .resume-project-sector {
    margin: 0;
    color: #6ee7b7;
    font-weight: 700;
  }

  .resume-project-period {
    font-family: "Chakra Petch", sans-serif;
    font-size: 0.84rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .resume-project-sector {
    font-size: 0.94rem;
  }

  .resume-project-card h3 {
    margin: 0.15rem 0 0;
    font-size: clamp(1.35rem, 2vw, 1.8rem);
    line-height: 1.15;
    color: #ecfdf5;
  }

  .resume-project-meta {
    display: grid;
    gap: 0.75rem;
  }

  .resume-project-meta > div {
    padding: 0.8rem 0.9rem;
    border-radius: 14px;
    border: 1px solid rgba(167, 243, 208, 0.12);
    background: rgba(4, 20, 16, 0.42);
  }

  .resume-project-body {
    display: grid;
    gap: 1rem;
    padding: 1.18rem 1.18rem 1.18rem 1.3rem;
  }

  .resume-project-body > * + * {
    padding-top: 1rem;
    border-top: 1px dashed var(--resume-divider);
  }

  .resume-project-copy {
    display: grid;
    gap: 0.8rem;
  }

  .resume-project-copy p {
    margin: 0;
  }

  .resume-detail-card {
    padding: 0.95rem 1rem;
    border: 1px solid rgba(167, 243, 208, 0.12);
    border-radius: 16px;
    background: rgba(4, 20, 16, 0.46);
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.03);
  }

  .resume-detail-card h4 {
    margin: 0 0 0.7rem;
    font-size: 1.14rem;
    color: #ecfdf5;
  }

  .resume-detail-card ul {
    display: grid;
    gap: 0.55rem;
  }

  .resume-stack-list {
    display: grid;
    gap: 0.75rem;
  }

  @media (min-width: 760px) {
    .resume-hero {
      grid-template-columns: minmax(0, 1.14fr) minmax(19rem, 0.86fr);
      align-items: stretch;
    }

    .resume-hero-media {
      min-height: 100%;
    }

    .resume-top-grid {
      grid-template-columns: minmax(0, 1.2fr) minmax(0, 0.8fr);
    }

    .resume-top-grid > * + * {
      padding-top: 0;
      padding-left: 0;
      border-top: 0;
      border-left: 0;
    }

    .resume-facts {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 0.9rem 1rem;
    }

    .resume-facts div {
      padding: 0.7rem 0.8rem;
      border-radius: 14px;
      border: 1px solid rgba(167, 243, 208, 0.12);
      background: rgba(4, 20, 16, 0.42);
    }

    .resume-facts div + div {
      border-top: 0;
      padding-top: 0.7rem;
    }

    .resume-skills-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .resume-project-header {
      grid-template-columns: minmax(0, 1.45fr) minmax(260px, 0.95fr);
      align-items: start;
    }

    .resume-project-meta {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      align-self: start;
    }
  }

  @media (min-width: 980px) {
    .resume-sheet {
      padding: 1.7rem;
    }

    .resume-skills-grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }

    .resume-project-body {
      grid-template-columns: minmax(0, 1.45fr) minmax(0, 0.9fr) minmax(0, 0.9fr);
      align-items: start;
    }

    .resume-project-body > * + * {
      padding-top: 0;
      padding-left: 1rem;
      border-top: 0;
      border-left: 1px dashed var(--resume-divider);
    }
  }

  @media (max-width: 760px) {
    .resume-toolbar-shell {
      padding: 0.9rem;
    }

    .resume-toolbar-actions {
      width: 100%;
      justify-content: stretch;
    }

    .resume-action {
      flex: 1 1 14rem;
    }
  }

  @media (max-width: 640px) {
    .resume-page {
      --resume-page-padding: 0.8rem;
    }

    .resume-toolbar-note {
      font-size: 0.95rem;
    }

    .resume-action {
      flex: 1 1 100%;
    }

    .resume-facts {
      gap: 0.75rem;
    }
  }

  @media (max-width: 520px) {
    .resume-page {
      --resume-dock-overlap: 8.25rem;
    }
  }

  @page {
    size: A4;
    margin: 12mm;
  }

  @media print {
    :root {
      --resume-print-page-bg: #ffffff;
      --resume-print-sheet-bg: #ffffff;
      --resume-print-panel-bg: #ffffff;
      --resume-print-panel-muted-bg: #ffffff;
      --resume-print-chip-bg: #f4f7f6;
      --resume-print-border: rgba(0, 0, 0, 0.16);
      --resume-print-border-soft: rgba(0, 0, 0, 0.12);
      --resume-print-heading: #0f172a;
      --resume-print-text: #1f2937;
      --resume-print-muted: #475569;
      --resume-print-accent: #0f766e;
      --resume-divider: rgba(0, 0, 0, 0.12);
      --resume-divider-strong: rgba(0, 0, 0, 0.16);
    }

    :root[data-resume-output="download"] {
      --resume-print-page-bg: #071712;
      --resume-print-sheet-bg:
        linear-gradient(90deg, rgba(220, 252, 231, 0.02) 1px, transparent 1px) 0
          0 / 24px 100%,
        linear-gradient(0deg, rgba(220, 252, 231, 0.018) 1px, transparent 1px) 0
          0 / 100% 24px,
        linear-gradient(135deg, rgba(7, 30, 24, 0.98), rgba(5, 24, 19, 0.94));
      --resume-print-panel-bg: rgba(6, 31, 24, 0.92);
      --resume-print-panel-muted-bg: rgba(9, 39, 30, 0.96);
      --resume-print-chip-bg: rgba(6, 33, 25, 0.95);
      --resume-print-border: rgba(167, 243, 208, 0.26);
      --resume-print-border-soft: rgba(167, 243, 208, 0.18);
      --resume-print-heading: #ecfdf5;
      --resume-print-text: #def7eb;
      --resume-print-muted: #bfe8d5;
      --resume-print-accent: #86efac;
      --resume-divider: rgba(167, 243, 208, 0.18);
      --resume-divider-strong: rgba(167, 243, 208, 0.26);
    }

    html,
    body {
      background: var(--resume-print-page-bg) !important;
    }

    body {
      -webkit-print-color-adjust: exact;
      print-color-adjust: exact;
    }

    .print-hidden {
      display: none !important;
    }

    .resume-page {
      margin-top: 0;
      padding: 0;
      background: var(--resume-print-page-bg);
      color: var(--resume-print-text);
    }

    .resume-sheet {
      width: 100%;
      max-width: none;
      margin: 0;
      padding: 10mm 10mm 9mm;
      border: 1px solid var(--resume-print-border);
      border-radius: 20px;
      box-shadow: none;
      backdrop-filter: none;
      background: var(--resume-print-sheet-bg);
    }

    .resume-hero {
      border-bottom-color: var(--resume-print-border-soft);
    }

    .resume-hero-media {
      min-height: 54mm;
      background: transparent;
      box-shadow: none;
      break-inside: avoid;
      page-break-inside: avoid;
    }

    .resume-hero-media::after {
      background: none;
    }

    .resume-panel::before,
    .resume-panel::after,
    .resume-project-card::before,
    .resume-project-card::after,
    .resume-skill-card::before,
    .resume-skill-card::after {
      display: none;
    }

    .resume-panel,
    .resume-project-card,
    .resume-skill-card,
    .resume-detail-card,
    .resume-link-list a {
      background: var(--resume-print-panel-bg) !important;
      border-color: transparent !important;
      box-shadow: none;
      break-inside: avoid;
      page-break-inside: avoid;
    }

    .resume-panel-emphasis,
    .resume-project-header {
      background: var(--resume-print-panel-muted-bg) !important;
      border-color: var(--resume-divider) !important;
    }

    .resume-chip-list li {
      background: var(--resume-print-chip-bg) !important;
      border-color: transparent !important;
      color: var(--resume-print-text);
    }

    .resume-badge,
    .resume-facts dt,
    .resume-project-meta dt,
    .resume-stack-list dt,
    .resume-project-period,
    .resume-project-sector {
      color: var(--resume-print-accent);
    }

    .resume-hero h1,
    .resume-section h2,
    .resume-panel h2,
    .resume-project-card h3,
    .resume-detail-card h4,
    .resume-skill-card h3,
    .resume-link-list a span:first-child {
      color: var(--resume-print-heading);
    }

    .resume-intro,
    .resume-facts dd,
    .resume-project-meta dd,
    .resume-stack-list dd,
    .resume-link-list a span:last-child,
    .resume-section-heading p,
    .resume-skill-card li,
    .resume-detail-card li,
    .resume-project-copy p {
      color: var(--resume-print-muted);
    }

    .resume-skill-card li::before,
    .resume-detail-card li::before {
      background: var(--resume-print-accent);
    }

    .resume-section {
      margin-top: 0.8rem;
    }

    .resume-projects {
      gap: 0.8rem;
    }

    a {
      color: inherit;
      text-decoration: none;
    }
  }
</style>
